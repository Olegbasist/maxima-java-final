openapi: 3.0.3
info:
  title: Booking API
  description: Booking API service
  version: 1.0.0
servers:
  - url: 'http://localhost:8080'
paths:
  /customer:
    get:
      responses:
        "200":
          description: URL and requests json schema
          content:
            application/json:
              schema:
                type: string

  /customer/all_customers:
    get:
      responses:
        "200":
          description: All customres list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: number
                    name:
                      type: string
                    contacts:
                      type: string
                    tickets:
                      type: array
                    


  
